<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agendamentos - CONSULT_MED</title>
</head>
<body>
    <h1>Listar Agendamentos - CONSULT_MED</h1>
    
    <ul>
        {{#each agendamentos}}
            <li>
                <a href="/agendamentos/{{id_ag}}">{{nome}}</a>
                <a href="/agendamentos/{{id_ag}}/editar">Editar</a>

                <form action="/agendamentos/{{id_ag}}/excluir" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Confirmar exclusão?')">
                        Excluir
                    </button>
                </form>
            </li>
        {{/each}}
    </ul>

    <a href="/">Voltar</a>
</body>
</html>