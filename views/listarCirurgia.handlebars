<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cirurgia - HOSPITAL_MED</title>
</head>
<body>
    <h1>Listar Cirurgias - HOSPITAL_MED</h1>
    
    <ul>
        {{#each cirurgias}}
            <li>
                <a href="/cirurgias/{{id}}">ID Cirurgia: {{id}}</a>
                <a href="/cirurgias/{{id}}/editar">Editar</a>

                <form action="/cirurgias/{{id}}/excluir" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Confirmar exclusão?')">
                        Excluir
                    </button>
                </form>
            </li>
        {{/each}}
    </ul>

    <a href="/">Voltar</a>
</body>
</html>