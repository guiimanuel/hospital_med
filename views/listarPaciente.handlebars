<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paciente - HOSPITAL_MED</title>
</head>
<body>
    <h1>Listar Pacientes - HOSPITAL_MED</h1>
    
    <ul>
        {{#each pacientes}}
            <li>
                <a href="/pacientes/{{id}}">ID:{{id}}</a>
                <a href="/pacientes/{{id}}">{{nome}}</a>
                <a href="/pacientes/{{id}}/editar">Editar</a>

                <form action="/pacientes/{{id}}/excluir" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Confirmar exclusão?')">
                        Excluir
                    </button>
                </form>
            </li>
        {{/each}}
    </ul>

    <a href="/">Voltar</a>
</body>
</html>