<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pacientes - CONSULT_MED</title>
</head>
<body>
    <h1>Listar Pacientes - CONSULT_MED</h1>
    
    <ul>
        {{#each pacientes}}
            <li>
                <a href="/pacientes/{{id_p}}">{{nome}}</a>
                <a href="/pacientes/{{id_p}}/editar">Editar</a>

                <form action="/pacientes/{{id_p}}/excluir" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Confirmar exclusão?')">
                        Excluir
                    </button>
                </form>
            </li>
        {{/each}}
    </ul>

    <a href="/">Voltar</a>
</body>
</html>