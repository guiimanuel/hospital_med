<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Médicos - CONSULT_MED</title>
</head>
<body>
    <h1>Listar Médicos - CONSULT_MED</h1>
    
    <ul>
        {{#each medicos}}
            <li>
                <a href="/medicos/{{id_med}}">{{nome}}</a>
                <a href="/medicos/{{id_med}}/editar">Editar</a>

                <form action="/medicos/{{id_med}}/excluir" method="post" style="display: inline;">
                    <button type="submit" onclick="return confirm('Confirmar exclusão?')">
                        Excluir
                    </button>
                </form>
            </li>
        {{/each}}
    </ul>

    <a href="/">Voltar</a>
</body>
</html>